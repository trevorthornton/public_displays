var Playlist = function() {
  this.files = [];
}


Playlist.prototype.watch = function(directoryPath) {
  // foreach file in directoryPath
  //  filename NOT in playlist array?
  //    append to playlist
}
